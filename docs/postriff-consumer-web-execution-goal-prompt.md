# Copy-ready Codex goal prompt: PostRiff consumer web product

Prepared 2026-09-15. This is an execution prompt, not an implementation receipt or authorization for external effects. Copy everything below the divider into a new Codex task. “PostDiff” in the request is treated as **PostRiff**, the product named in the architecture specification.

---

You are the lead engineer and product delivery agent for **PostRiff Cloud**, the consumer-ready website/PWA. Work in `/Users/ouxianxing/Documents/James-Au-Studio`. Your objective is to deliver the **first sellable, independently hosted PostRiff web product** described in `docs/superpowers/specs/2026-09-14-postriff-saas-product-architecture.md`, especially sections 5–18, 20–28, and 31. The website must work when James's laptop and local Studio are offline. Preserve PostRiff Remote as a separate founder alpha. Do not make a customer's browser or James's personal Codex/ChatGPT session the SaaS runtime.

This is a sustained implementation goal. Do the work, not just an audit, plan, wireframe, or collection of recommendations. Continue through multiple turns and milestones until all work within the authorized scope is implemented, verified, and receipted. If the Codex goal facility is available, create the explicit goal “Deliver the consumer-ready PostRiff Cloud website/PWA against the architecture acceptance criteria,” and mark it complete only when the applicable criteria genuinely pass. Do not mark the goal complete because a token budget, turn, or local milestone ended. Use a milestone checklist and a durable handoff receipt so another turn can resume without repeating work.

## Authority and execution boundary

I authorize read-only checks, scoped local code/documentation changes, additive migrations in disposable/local test databases, build/test/browser validation, and reversible repairs required for this objective. Do not pause to ask me for routine implementation choices. Reuse an approval already given for the same exact action and scope; do not ask twice. Continue all independent authorized work while an external gate is pending.

Before any **new** production/staging deployment, real database migration, production OAuth/app-review submission, live provider connection, social post/reply/moderation, paid model/media generation, billing activation/charge, customer invitation/message, sensitive customer-data upload, credential/security change, deletion, or publication, prepare the concrete result and exact action preview, then obtain the required target/content/access/cost approval. Existing credentials show capability, not consent. Approval is the final step after the candidate, tests, rollback, and expected result are reviewable. Never infer that this prompt alone authorizes a new external effect. Do not create recurring approval reminders unless I request them. If an approval is unavailable, finish the independent engineering, label the affected gate accurately, and leave the precise action preview; do not claim consumer readiness.

Never execute a real social action merely to prove a test without exact account, content, timing, and destination authorization. Provider-accepted, published, and independently verified are different states. Do not use a synthetic account, fixture checkout, mock OAuth, procedural artwork, prepared request, protected preview, or HTTP 200 as proof of a real customer journey.

## Source of truth and starting audit

Read applicable `AGENTS.md`/local operating rules, the current source, and the newest Phase 0–3 receipts before editing. Read these three exact sources in full and cite them in the requirements ledger:

1. `docs/superpowers/specs/2026-09-14-postriff-product-design.md` — six-destination customer experience, Ideas, previews, preflight, scheduling, Channels, Analytics, Audience, mobile, and accessibility.
2. `docs/superpowers/specs/2026-09-14-postriff-saas-product-architecture.md` — cloud tenancy, security, agent/tools, connectors, durable execution, billing, operations, and release gates.
3. `docs/superpowers/plans/2026-09-14-postriff-foundation-and-ideas.md` — task-level Ideas/navigation/domain/event/preview/action-handoff implementation checks. Audit its checkbox tasks against current code; reuse relevant unfinished contracts and tests rather than mechanically replaying already delivered tasks.

Also read `docs/superpowers/specs/2026-09-14-postriff-content-type-template-system.md`, the controlling consumer product plan at `/Users/ouxianxing/Documents/Codex/2026-09-14/ok-just-to-continue-from-the/postriff-product-plan/`, and `docs/postriff-improvement-20260914/` for newer product, economics, source-policy, and phase decisions. Use the latest explicit user decision first; current verified code/receipts establish what exists; the consumer plan governs newer phase numbering and packaging; the architecture spec governs the web requirements and acceptance criteria. The Ideas plan's FastAPI/SQLite local service and version-pinned local Codex app-server broker are transitional founder architecture, not the paying-customer cloud topology or credential strategy. Adapt its UI/domain contracts to tenant-safe PostRiff Cloud instead of copying its local-only runtime. When documents conflict, record the decision and why. Do not silently revive superseded prices, UI requirements, or phase labels.

The installed tree previously lacked Git metadata. Check again. If absent, report `validation_unavailable: Git history/diff unavailable because this installed source has no .git`; preserve scoped pre-edit backups, hashes, and a reviewable source diff. Do not initialize Git or overwrite concurrent work just to satisfy a workflow. Audit live processes, cloud deployment status, environment scope, migrations, provider capabilities, and current tests instead of treating historical receipts as confirmed-current. Inspect secrets only by presence/metadata where possible; do not print them in chat, logs, or artifacts.

### Project map: inspect these files before designing replacements

Start with this curated map, then follow imports/call sites as needed. Cite the files actually used in the requirements ledger; file existence by itself is not a passing requirement.

| Purpose | Files and what to check |
|---|---|
| Current truth and inherited gates | `docs/postriff-phase-2/phase-2-receipt.md`, `docs/postriff-phase-2/acceptance-results.md`, `docs/postriff-phase-2/connector-qualification.md`, `docs/postriff-phase-3/phase-3-receipt.md`, `docs/postriff-phase-3/runtime-qualification.md`, `docs/postriff-phase-3/sync-recovery-receipt.md`. Distinguish hosted synthetic production acceptance from real AI/social execution and local desktop recovery from deployed sync. |
| Newer product/safety decisions | `docs/postriff-improvement-20260914/DECISIONS.md`, `SPEC.md`, `ECONOMICS.md`, and `ROADMAP.md`. Carry forward source-first activation, four-class source egress, approval/reconciliation repairs, bounded cost, and evidence-led rollout; do not treat proposed prices or simulated economics as sales evidence. |
| Hosted deployment/API | `vercel.json`, `api/index.py`, `src/postriff_phase2/hosted_app.py`, `hosted.py`, `hosted_identity.py`, `hosted_storage.py`, and `hosted_worker.py`. These show the actual Vite/Python services, Supabase session/workspace boundary, private assets, cron worker, and currently disabled social execution. Check current production configuration and exclude boundaries without printing `.env.local` or secrets. |
| Data/security migrations | `migrations/postriff/001_phase2.sql`, `002_hosted_account_lifecycle.sql`, `003_phase3_runtime.sql`; `docs/postriff-phase-2/security-and-rls-results.md`; `tests/phase2/rls.sql`, `postgres_safety.py`, `postgres_repository.py`; `tests/phase3/postgres.py`. Reuse the additive tables and two-user RLS/repository checks; inspect whether migration 003 is actually hosted before building on it. |
| Approval/provider execution | `src/postriff_phase2/contracts.py`, `store.py`, `outcomes.py`, `provider_candidates.py`; `docs/postriff-phase-2/approval-and-state-model.md`; `tests/test_postriff_safety_regressions.py`. Preserve source/account/revision-bound manifests, exact receipts, fail-closed transport, and reconciliation-before-retry. A request candidate is not a qualified live connector. |
| Agent/runtime boundary | `src/postriff_phase3/contracts.py`, `adapters.py`, `worker.py`, `hosted.py`, `transport.py`; `scripts/postriff_agent_tools.py`; `docs/postriff-phase-3/security-sync-acceptance.md`; `tests/test_postriff_phase3.py`. Identify which runtime work is local, fixture, candidate, or mounted cloud behavior before exposing a consumer route. |
| James Au orchestrator and product skill provenance | `/Users/ouxianxing/.codex/skills/james-au-social-orchestrator/SKILL.md`, its `references/routing-and-safety.md` and bundled `references/james-au-social-content-engine.md`; `docs/postriff-phase-1/skill-migration-manifest.md`; `docs/postriff-phase-1/released-templates/social-agency-orchestrator/SKILL.md`; `src/postriff_alpha/templates.py`. Read the private orchestrator as a **founder workflow reference** for canonical brief, one-question intake, source/viewpoint separation, selective adapter routing, per-channel states, and approval/verification gates. Audit its dependencies and founder-specific first-run/browser/account paths before reusing ideas. The separately authored neutral alpha template is not a complete conversion or production-qualified customer skill. |
| Customer web UI and API | `studio/web/src/founder/FounderApp.tsx`, `Phase2.tsx`, `RuntimePanel.tsx`, `ContentTypes.tsx`, `AuthEntry.tsx`, `hosted-auth.ts`, `api.ts`, `YouProfile.tsx`; `studio/web/package.json`; `studio/web/tests/runtime-recovery.test.ts`. Preserve real draft/profile/type/approval behavior, distinguish hosted from local labels, and find fixture-only Plan/Channel/Agent states. |
| Existing test/deployment commands | `tests/test_postriff_phase2_hosted.py`, `test_postriff_hosted_deployment.py`, `test_postriff_content_types.py`, `test_postriff_phase3.py`; `scripts/check_postriff_hosted_preflight.py`, `validate_postriff_hosted_preview.py`, `verify_postriff_phase2.py`; `.env.example`. Run only the validators applicable to the changed boundary and verify a production route separately from a build or protected preview. |
| Research and metric definitions | `docs/postriff-phase-0/evidence-matrix.md` and `docs/superpowers/specs/2026-09-14-postriff-admin-analytics-design.md` when implementing activation, Analytics, Audience, or operator reporting. Preserve the distinction between zero completed full interviews, synthetic users, actual cohorts, native platform metrics, and missing provider data. |

The existing `docs/postriff-phase-4-execution-prompt.md` is a candidate prompt for a narrower phase, not proof that Phase 4 ran. Use it only to reconcile newer UI/agent/billing decisions with this consumer-web objective; do not mechanically execute its Halaska or pilot instructions when they do not help the current critical path.

Create `docs/postriff-consumer-web/requirements-ledger.md` before changing the application. Map **every architecture section 1–32** to one of: `implemented_and_verified`, `partially_implemented`, `blocked_external_gate`, `deferred_by_first_sellable_scope`, or `not_applicable_to_web`. For every section, cite the concrete code/UI/API/test/evidence or the exact missing work. In particular, map every bullet in sections 27 and 28 individually. A checklist item is not complete without its relevant validation and observed execution state. Update the ledger after each milestone.

## Delivery target and sequencing

Build the first sellable cloud web product for solo creators first, with truthful limited Analytics/Audience where provider permissions do not support more. The required paid-beta scope is section 27: cloud signup and workspace; source-aware Ideas conversation; personalized content-type catalog, guided creation, starter packs, and private/workspace templates; relevant skill routing and controlled Python tools; canonical brief and at least two native variants; image upload/generation, alt text, and preview; preflight; list/calendar scheduling; **two or three production-qualified direct integrations** selected from a current official-provider capability/app-review audit; exact approval and per-destination receipts; basic post performance where available; usage/credits, real subscription state, export, disconnect, deletion; responsive installable PWA. Do not expand to 33 channels, native apps, unrestricted browser automation, unlimited generation, or automatic replies to make the release seem larger.

Order implementation by dependency and customer value. First protect tenant/source/agent/cost boundaries; then complete the real source-to-draft web journey; then qualify two or three connectors and durable execution; then billing/account lifecycle; then finish mobile, operations, and end-to-end launch acceptance. Do not let a large visual redesign or optional desktop work delay the core customer journey. Preserve existing work, revisions, profile/voice controls, content-type contracts, preflight, approvals, receipts, and local founder paths. Use additive migrations and backward-compatible API/event changes wherever feasible.

### Milestone A — secure SaaS foundation

Implement and verify customer authentication, verified identity, secure server sessions, workspace creation/switching, owner/admin/editor/approver/viewer membership, invitations, separate publish/reply/moderate/manage-connection permissions, session revoke/recovery, throttling, and step-up for sensitive actions. Every customer-owned record and object is workspace-scoped; enforce context in service code and database RLS, not a client-supplied body field. Use private object storage and short-lived workspace-bound signed URLs. Job claims re-authorize membership and entitlement. Agent retrieval and tool mounts contain only selected workspace inputs. Separate service identities and server-side secrets; never forward provider tokens to the agent or browser. Prove with two synthetic tenants that cross-tenant enumerate, read, inference, mutation, stream, export, object, job, and error paths fail without revealing existence. Test role revocation during active runs/jobs.

### Milestone B — real cloud Ideas and agent journey

Implement the persistent PostRiff Agent through a product-owned `AgentRuntime` interface: start/resume conversation, start/cancel turn, stream safe events, and list supported models/reasoning. Use a production-qualified **server-side** runtime identity, separate from James's personal sessions. Support Quick/Standard/Deep where actually qualified; show limited/unavailable routes honestly. Client events are restricted to the safe families in section 10.4, with stable IDs/cursors and replay/deduplication; never expose raw runtime events, hidden reasoning, prompts, keys, shell output, or arbitrary file paths. Save conversations, source manifests, partial runs, artifacts, revisions, model/tool/skill provenance, and usage by workspace/member/run. A Dashboard agent room may reuse the canonical Ideas conversation; it must not create a second draft authority or allow a chat sentence to publish, reply, connect, buy, or delete.

Implement source-first activation: a new user can enter a thought, URL, permitted document, image/media, or transcript and reach one useful preview without connecting a channel. Record verified facts separately from user viewpoint, audience, language, and goal. Enforce the newer four-class source/egress policy in `docs/postriff-improvement-20260914/SPEC.md`: labels alone are insufficient; private/local-only or retracted material must never leave its permitted boundary or remain in generated/retrieved descendants. Treat source content and attachments as untrusted instructions. Generate a canonical brief and at least two independently editable platform-native variants, preserving source citations, cultural/language choices, voice revision, type/template version, and destination constraints. Implement independent edit/review/remember/post-only/reject/undo controls without silent memory mutation. The personalized catalog, optional starter packs, guided one-question-at-a-time type creation, proposal review, private/workspace templates, and format layer must work on desktop and mobile.

Wrap approved Python capabilities behind versioned structured tools with input/output schemas, reviewed release IDs, bounded CPU/memory/time/network/artifact scope, fresh isolated workspace, cost/effect class, cancellation, and audit-safe summaries. Unknown client-supplied tool/skill IDs fail closed. Separate creative drafts from external executors. Verify the actual isolation boundary; a Python function called inside an unrestricted request process is not an isolated runner. If real isolation is not available, block the public tool route and record the gap. For Vercel external integrations, load the applicable Marketplace guidance and run discovery before selecting/provisioning a new service; use official current docs and a reviewable integration decision rather than an old example.

Use the James Au social orchestrator only to **extract and test product-worthy workflow contracts**. Release customer-safe, brand-neutral PostRiff skills as versioned resources with schemas, hashes, dependency/effect review, fixtures, and workspace configuration. Keep James's private Content Engine, personal voice/brands, owner-only first-run store, channel identities, browser sessions, credential handoffs, Agent Reach configuration, and installed skill bodies out of customer bundles, prompts, tool mounts, and global product catalogs. A customer may supply and approve their own voice/source guidance. Founder-only commands or approvals never grant a customer's agent a connector, research backend, or publication capability. Preserve the orchestrator's distinction between skill loaded, source acquired, claims verified, a candidate drafted, account identity checked, action approved, submitted, and independently verified; test those distinctions through PostRiff-owned contracts.

Support private uploads and immutable generated/rendition assets with provenance, rights/attribution declaration, AI labels where needed, format/dimension/size validation, alt text, and short-lived previews. Generated image/media requests must pass the credits/cost gate before execution. Use real generation only after exact provider/account/payload/cost authorization; otherwise finish the integration and tests but keep it blocked. Complete platform preview and smart preflight with issue severity and binding to the exact revision/action manifest.

### Milestone C — channels, scheduling, publication, receipts

Run a fresh current official-provider audit and select two or three realistic launch connectors by verified direct publish/schedule capability, app-review path, media support, analytics feasibility, cost, and recovery behavior. A local James Au Studio grant is not a cloud customer grant. For each connector, implement PostRiff-owned public HTTPS OAuth callback, state bound to workspace/member/provider/capability/redirect/expiry, PKCE where applicable, exact account confirmation, incremental scopes, encrypted token custody, refresh/expiry/revocation, scope drift, disconnect, and capability-specific probes. Show separate identity, publish, schedule, analytics, comments-read, reply, moderation, media, and webhook states as Direct, Assisted, Bridge, or Unsupported. Do not expose a connector publicly until its provider review/production scope and end-to-end account evidence pass. If a connector fails review, keep Assisted export truthful and select another only after a new audit.

Make schedules durable across API/worker restarts and founder-laptop shutdown. Store intended local time, IANA timezone, resolved UTC, timezone database version, content/account revision, and DST ambiguity handling. Use claims/leases and re-authorization. Immutable approval manifests bind workspace/approver, action, exact account IDs/handles, brief/variant revisions, ordered media hashes/alt text/options, preflight hash, timing, expiration, and idempotency key. Content, source, account, capability, or timing drift invalidates approval. Customer buttons name the exact action and destination count. Execute only supported destinations through dedicated connector workers. Preserve per-destination `not_attempted`, `blocked`, `queued`, `leased`, `submitting`, `submitted`, `published`, `verified`, `failed`, `needs_reconciliation`, and `cancelled`; a provider HTTP success cannot become verified without its defined evidence. Never retry an uncertain submission before provider reconciliation. Test partial success, crash/lease recovery, duplicates, rate limits, revocation during execution, and honest receipts. Obtain exact action approval before any real publication.

### Milestone D — billing, privacy, Analytics, Audience

Implement workspace subscription/entitlement state, trial and plan terms as versioned decisions, an append-only idempotent usage/cost ledger, reserve/settle/release, per-model/media/tool attribution, quotas and global stop limits, understandable remaining credits/reset/overage behavior, and a real `Usage & Plan` surface. Do not advertise unlimited generation or charge overages silently. Before selecting a payment provider on Vercel, load Marketplace integration guidance/discover and verify current provider, tax/jurisdiction, webhooks, refunds, cancellation, and exit constraints. Keep provider-specific code behind an adapter. Verify webhook signatures, replay/out-of-order/idempotent processing, entitlement reconciliation, cancellation/grace/downgrade/failed-payment/export/deletion behavior. A fixture adapter is labelled fixture; live subscription and charge remain blocked until separately authorized and observed.

Implement privacy notice, customer rights declaration, retention classes, sanitized support diagnostics by consent, data export with receipt, deletion/tombstone/backup-restore behavior, provider disconnect/revocation, source retraction and derivative cleanup, and no cross-tenant content in telemetry. Privacy/terms/tax/regulatory review requires qualified professional evidence before public sale; draft documents alone are not a legal approval.

Analytics must store native metric definitions, observation/ingestion times, provider identity, availability, and freshness. Missing data shows Unavailable, not zero; cross-platform reach is never unique people; rates show numerator/denominator; incompatible cohorts do not compare; under three comparable posts shows insufficient sample. Audience must show supported original thread context, manually authored or labelled AI reply drafts, exact account/thread/final-text approval, separate send/verification receipts, and capability-dependent limitations. A provider without comment/reply access gets a truthful limited state, not simulated live conversations. Automated/bulk replies and moderation remain separately gated later features.

### Milestone E — web experience, operations, and launch acceptance

Keep exactly six primary destinations: Dashboard, Ideas, Scheduling, Channels, Analytics, Audience. Workspace/team/billing/usage/settings/audit/Bridge/help/export/security belong in utility navigation. Desktop has a collapsible six-destination sidebar; mobile has Dashboard, Ideas, Scheduling, Audience, plus More for Channels/Analytics. The global Create action opens Ideas. On mobile use Chat/Edit/Preview segments instead of squeezed desktop panes, 44×44 px targets, installable PWA, safe offline viewing of recent drafts, and truthful upload/share-sheet support. Push permission requires a user gesture and notifications only open resources; they never execute an action. Confirm all six destinations and core source-to-draft/approval/account flows at 390×844, keyboard and screen reader, focus, contrast, reduced motion, and cellular-network access independent of founder LAN.

Implement customer-visible connection/worker/incident status and analytics freshness; bounded sanitized logs/traces, OAuth/scope drift, queue/lease/reconciliation backlog, agent/cost failures, billing drift, feature flags to disable unsafe capabilities, backup restore tests, dependency/infrastructure scans, and incident/runbook paths. Do not log full customer prompts, post bodies, comments, tokens, or files by default. Complete plain-language onboarding that asks one compact question at a time, allows draft-first use, and explains requested provider permissions. A sample workspace cannot publish. Support paths must cover account recovery, payment, export/deletion, disconnect, and failed publication.

## Validation and release proof

After each milestone, run the smallest sufficient tests/builds/schema checks/browser checks for its concrete risks, repair failures, and update the ledger. Reuse passed checks for unchanged sources/configuration; rerun affected checks after changes. Include security tests for tenant isolation, source egress, upload handling, unsafe agent event/tool IDs, paid gate, role/approval drift, OAuth callback/token lifecycle, schedule DST and crash recovery, uncertain submission reconciliation, billing webhook replay, export/deletion and backup restore. Browser-test actual mobile and desktop journeys and visually inspect representative screens. Use synthetic accounts for broad negative tests; label them synthetic. Use explicitly approved limited real tests to qualify model/provider/billing routes, and preserve their exact receipts. If a necessary test cannot run, write `validation_unavailable` plus exact cause and best lower-level check; do not silently mark the criterion passed.

The final consumer-ready acceptance is an observed end-to-end cloud journey with the founder laptop off: a new permitted user signs up and verifies identity, creates a workspace, completes one-question onboarding, creates a source-aware real-model draft and at least two variants, uploads or generates permitted media, edits/preflights, connects one of the production-qualified accounts with verified scopes/token lifecycle, approves an exact schedule/publication, sees separate durable destination receipts and post-level analytics when available, checks true usage/subscription state, exports and can disconnect/delete. Repeat with a second workspace and prove isolation. Verify mobile PWA and failure/recovery paths. Record which parts were observed real, provider accepted, independently verified, synthetic, blocked, and unavailable. Two or three launch connectors must pass their individual production qualification gates before calling the first sellable scope complete.

## Durable output and honest stop condition

Maintain `docs/postriff-consumer-web/requirements-ledger.md`, `implementation-plan.md`, `decisions.md`, milestone receipts/evidence, exact external-action previews, and `consumer-web-release-receipt.md`. Each receipt states code/artifacts changed, validation and environment, actual external state, remaining gates, rollback/recovery, and the next concrete action. Preserve earlier receipts as history rather than rewriting them. Use clickable absolute paths in user reports. Give me concise progress updates at meaningful boundaries; do not disappear into a long silent run.

Do not say “consumer-ready,” “paid beta live,” “public launch,” or “all specifications complete” until the relevant real, cloud, security, connector, billing, legal/support, and mobile evidence passes. If an external approval, credential/account setup, provider review, professional legal/tax decision, or design-partner observation is required, finish all independent work first, then present the minimal exact request and its reviewable candidate. A design-partner interview, provider approval, real payment, or four-week retention observation cannot be manufactured by Codex. Keep the goal active with a precise handoff until that gate is resolved; do not substitute mocks or announce completion.

PostRiff Bridge/native desktop, all 33 connectors, advanced unique reach, agency hierarchy, automatic/bulk replies, and selective automation are later roadmap capabilities. For the website, implement truthful Assisted/Unsupported states and utility placeholders only where useful; do not count those deferred items as completed. If the user later explicitly expands scope to the full long-term architecture, plan and execute them as separate verified milestones.
