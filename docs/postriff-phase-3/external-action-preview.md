# Pending external action preview — NOT EXECUTED

An [exact prepared request body](managed-writing-prepared-review.json) and [follow-up receipt](runtime-followup-receipt.md) are now available. Account/key location has been requested; account, price and execution authorization remain pending.

Candidate: one managed-writing qualification request, separate from any deployment, pairing rollout or social action.

- Local environment: this Phase 3 source tree and a disposable fictional workspace. No Vercel/Supabase migration or upload in this preview.
- Provider: DeepInfra (`deepinfra/api-token`, discovered read-only). Endpoint: `https://api.deepinfra.com/v1/openai/chat/completions`.
- Model candidate: `meta-llama/Llama-3.3-70B-Instruct`. Its current availability, exact price and structured-output support must be verified on the selected account before arming; this is not a qualified model selection.
- Account/key target: a user-designated DeepInfra API account has not been identified or authorized. Supply a server-side protected credential only; never in chat, source, browser storage or exports.
- Device: current macOS arm64 development host; no personal CLI/browser sessions used.
- Synthetic inputs: fictional community garden, a free Saturday seed swap, visitors may bring seeds or learn, beginner planting demonstration. Speaker: fictional community educator. Tone: warm and clear; no lived-experience claim. Request an English LinkedIn text and Traditional Chinese Instagram caption, each with source IDs and unknowns. No person/account/real location/customer content.
- Limits: one request, no tool calls, no fallback/retries, at most 60 kB serialized approved context and 2,048 output tokens, 45-second HTTP timeout. Proposed maximum **US$0.05 total**. The adapter requires a fresh server-reviewed price quote and rejects a conservative token ceiling above that amount. No current price is claimed because primary model-page retrieval failed.
- Review exact input hash, voice/type/template/skill versions, model, account and price quote before authorization. A pairing credential or subscription is not authorization to spend.
- Expected evidence: provider-reported usage, schema-valid two-language candidates, source/provenance checks, independent edit/save/restart, cancellation/ambiguous-result behavior. HTTP parser fixtures do not count.
- Cleanup: remove only the named synthetic local workspace/export after review; revoke only a credential created for this test if requested. Provider-incurred charges and retention cannot be undone by deleting local files. Never retry an uncertain request without reconciliation and new cost authorization.

This preview is pending target/model-price verification and explicit authorization; no question or approval reminder was sent. Codex/Gemini isolation qualification, Claude API qualification and hosted two-device rollout need their own bounded previews after their concrete prerequisites are ready.
