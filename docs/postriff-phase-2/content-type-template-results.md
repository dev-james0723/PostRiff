# Content-type and template increment results

2026-09-14. This evidence covers local deterministic and disposable PostgreSQL execution only.

## Implemented contract

- Five neutral PostRiff content types and an optional, versioned `pack.creator` with 11 founder types.
- Exactly ten format families, kept separate from content type and destination.
- Workspace-specific catalogs and at most four context suggestions; unrelated restaurant workspaces receive restaurant suggestions without inheriting the Creator pack.
- Four creation paths: one-question guided intake, example analysis, released-type adaptation, and manual definition.
- Editable proposal and synthetic comparison before an exact-digest confirmation can save a workspace type.
- Existing sources and customized variants survive a type change; legacy drafts remain `unclassified`.
- Private templates default to owner-only visibility. Explicit workspace sharing, duplicate, revision-checked edit, archive, restore, version history, and safe export are covered.
- Content type, version, format, preflight result, and neutral route IDs are bound into approval manifests.
- Public catalog endpoints expose 16 safe definitions, ten formats, and seven pack descriptors without private instructions.

## Validation receipt

| Check | Result |
|---|---|
| Python unit and acceptance suite | PASS, 85 tests |
| Frontend Node suite | PASS, 70 tests |
| TypeScript | PASS |
| Production and founder-alpha Vite builds | PASS |
| Disposable PostgreSQL repository and worker recovery | PASS, psycopg 3.3.5, no duplicate submit |
| Fresh-cluster RLS and private Storage policy | PASS, two users, nine object families, forged-ID and CRUD denials, revoked member |
| Public local APIs | PASS, 16 content types, 10 formats, 7 packs |
| Browser console | PASS, zero warnings or errors |

## Browser inspection

Desktop inspection showed the optional Creator pack expanding the workspace catalog from five to 16 entries, with version and provenance on every card. The guided builder displayed one question at a time, then a clearly unsaved proposal with editable name, purpose, exclusion rule, structure, synthetic examples, and comparison controls. Exact confirmation created `A Weekly Studio Build Log` under a workspace namespace. Selecting it preserved one source and one customized variant. Saving `Weekly studio log` produced a revision-1 private template.

The same proposal review was inspected at a 390 by 844 viewport. The modal became a full-height single-column sheet, all fields remained readable, and the stable Close action stayed reachable. The viewport was reset afterward.

## Boundaries

The running app reports `phase2-local-fixtures`, `phase0: incomplete`, and `customerValidated: false`. Browser actions used the local deterministic workspace. No provider account, payment, paid generation, deployment, social publication, message, or customer interview occurred.
