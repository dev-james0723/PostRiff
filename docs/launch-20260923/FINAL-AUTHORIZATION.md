# FINAL authorization list — 2026-09-24

Everything below is **not done** and needs an explicit yes from the owner before it runs. Local work that did
not need authorization is finished and recorded in `FINAL-LEDGER.md`. Existing keys and configuration are reused
where they already exist; nothing here asks for a key to be pasted into chat, and no key leaves its store.

| # | Action | Exactly what would run | Worst case | Why local tests cannot replace it | Unblocks |
|---|---|---|---|---|---|
| 1 | **Live model qualification** (FINAL-04) | With the existing `AI_GATEWAY_API_KEY`, on a disposable local database and a fictional workspace: 3 models from different makers that the gateway lists for the account (proposed `anthropic/claude-sonnet-5`, one OpenAI and one Google text model — confirm the IDs), each: English caption, Traditional Chinese (HK) caption, 2-account/2-language draft, personalized-voice draft, one deep draft = 15 drafting requests; plus 1 image (`POSTRIFF_IMAGE_MODEL`) and 1 voice analysis | spending cap **US$3.00** total (sum of the ceilings is about US$1.2 at current defaults; the cap is the hard stop) | whether `providerOptions.gateway.only` is honoured on each route, where routing/cost metadata appears in the raw response, real `finish_reason`/usage shapes, zh-HK quality | FINAL-04 `liveModelVerification` |
| 2 | **Approve spending budgets** | In the target database, set `pr_budgets.status='approved'` with owner-chosen `warn`/`stop` caps for `global` and each workspace scope (today every budget is `candidate`, so paid drafting answers 402) | the caps you choose | production state was not inspected | paid drafting in any real environment |
| 3 | **Stripe Sandbox** (FINAL-06/07) | In your Stripe **test mode** account: register the webhook endpoint for a preview URL (or `stripe listen` to loopback), run one credit-pack Checkout with a test card, replay the event, refund it, open and close a test dispute, one subscription create + one renewal (test clock) with monthly credits | no money (test mode) | real Stripe signatures, the 2025+ invoice `payments` shape, dispute event ordering | `stripeSandboxVerification`, `monthlyCreditIssuance` |
| 4 | **Commercial terms** | Decide credit-pack prices, monthly credit amounts per plan, expiry, refund/dispute handling of spent credits, proration on upgrade/downgrade, trial credits | — | policy, not code | activating any price or credit flag |
| 5 | **Legal facts** | Merchant entity and address; governing law; privacy/support mailbox that exists; data regions (Vercel, Supabase, Resend, AI Gateway route, Exa, Jina); log/backup retention; Terms for credit packs, renewal and cancellation; counsel review before flipping `LEGAL_REVIEW_STATUS` to `reviewed` | — | facts only the owner/counsel can confirm | public or paid launch |
| 6 | **Live publishing check** (FINAL-09) | The package in `FINAL-CAPABILITY-MATRIX.md` §4: own LinkedIn member profile + own Threads profile (as tester), EN and zh-HK captions with `(`, `@`, a hashtag and a link, one Threads image; approve through the Queue; check permalinks; delete the posts afterwards | 4–5 public posts on your own accounts, deleted after the check | little-text escaping, container timing, receipts and permalinks on the real APIs | `livePublishingVerification` |
| 7 | **Platform reviews** | Meta App Review (+ Business Verification for Instagram) for non-tester users; LinkedIn keeps `r_member_social` closed | — | external review | publishing for customers who are not app testers |
| 8 | **Email** | Send one reminder/billing notice through Resend to your own address after the sender domain is verified | 1–2 emails | real delivery; local tests use a recording transport | notice delivery |
| 9 | **Production migrations** | First a read-only check of which of `013`, `020_credit_quotes`, `021_credit_purchases`, `022_credit_payment_lifecycle` are applied; then, only if approved and after a backup, apply the missing ones (all create tables/columns disabled by default) | schema change on the production DB | local disposable PostgreSQL only | any credit feature in production |
| 10 | **Git** | Commit the promoted files on a branch (staging by path, never the whole tree — other sessions share the checkout), push, open a PR | — | — | review and CI |
| 11 | **Deployment** | Preview deployment from an isolated staged source (release procedure in memory), then production only after 1–10 as needed | — | — | `productionDeployment` |

Standing rules kept throughout: no production DSN in tests, no price or credit flag activated, no post, email or
charge without the matching row above approved, no push or merge of shared branches, no secret printed.
