# FINAL-LEDGER (FINAL-00 to FINAL-12)

Last update: 2026-09-24 08:58Z (04:58 EDT). Integration writer: this Claude session (single writer for the candidate).
Status vocabulary: `NOT_STARTED | IMPLEMENTED_UNVERIFIED | LOCAL_VERIFIED | BLOCKED_EXTERNAL | NOT_IN_SELECTED_RELEASE`.
No item below is `NOT_IN_SELECTED_RELEASE`: no phasing has been approved by the user.

## Where things are

| | Canonical | Candidate (integration workspace) |
|---|---|---|
| Path | `/Users/ouxianxing/Documents/James-Au-Studio` | `/Users/ouxianxing/Documents/James-Au-Studio-launch-20260923` |
| Branch / HEAD | `consumer-saas` / `468811b` + ~280 dirty files | `codex/launch-audit-20260923` / `468811b` + dirty files |
| Other writers | Yes: a concurrent Rafii v9 session (Token Pilot session `69e3ce2f…`) edits `web/src` and runs visual evidence there | None besides this session |
| Latest candidate snapshot | — | FINAL-11 frozen snapshot `evidence/final/r2/snapshot-before.json`, manifestSha256 `038bfd82fdb6fbe22018efd9a7fe2b572e56fc03438dae322c42c14d05ea658d` (1,180 files, 07:51Z). Earlier: `r1/snapshot-after-final00.json` `1b7eca41…` (1,151 files) |

Processes (do not stop the ones this session did not start):

| Process | Owner | Notes |
|---|---|---|
| API `127.0.0.1:4461` + PG `55761`, later `4438` + PG `55479` (preview `launch-final-api` / `-legacy`) | this session | stopped 2026-09-24 ~09:05Z |
| web 4462 (`next dev`), 4439 (`next start` of the scratch build), diagnostics 4440/4441 | this session | all stopped; scratch build copies deleted |
| API 4359/PG 55469, API 4369/PG 55569, web 4599 (dist `.next-finish`) | previous launch run | left running; 4599+4359 reproduce the 22:24 sweep environment |
| canonical API 4331, web 3100 | other sessions | not touched |

## FINAL-00 — traceable candidate without overwriting others' work — `LOCAL_VERIFIED`

- Requirement: classify canonical/candidate differences against a real base, merge both-changed files hunk by hunk, keep canonical's latest v9 and unrelated work.
- Done:
  - `evidence/final/r1/classify.py` + `classification-r1.json`: bases taken from the most recent real sync point per file (`upstream-preserved.current` 22:17 → `merged-canonical-preimage` 22:13 → `canonical-refresh-base` 21:56 → `source-before` 18:45 → `HEAD`). Result: 879 canonical-only (829 are untracked canonical docs/evidence never in the candidate; 50 runtime), 465 identical, 54 candidate-only (52 runtime), 13 both-changed (12 runtime + 1 docs evidence json).
  - Real base content existed only for 3 of the 12 runtime both-changed files (HEAD). No three-way base was invented: HEAD (a true older ancestor) was used for `git merge-file` trials, and each hunk was attributed by comparing with HEAD and the candidate's pre-refresh backups.
  - `refresh-canonical-only.json`: 47 files taken verbatim from canonical, 2 dev-only files removed like canonical (`web/src/app/dev/content-library-dialog/*`), 0 skipped; backups in `evidence/final/r1/pre-refresh/`. `.claude/launch.json` not copied (local tool config).
  - Both-changed merges (backups in `evidence/final/r1/pre-merge/`):

| File | Resolution |
|---|---|
| `features/agent/plan.ts`, `web/tests/launch-plan-accounts.test.cjs` | candidate (= canonical + account-safe `defaultPlanAccount` and its test) |
| `features/workspace/brand-view.tsx` | canonical ("Rafii" spelling) + candidate's truthful page description ("AI suggestions need your approval") |
| `features/channels/channels-view.tsx` | canonical. Candidate's only change was trimming the description to "Manage your connected accounts.", which dropped "a connected account is not the same as a publishable one"; revisit in FINAL-03 |
| `language-picker/channel-language-chip.tsx` | canonical (`label` prop supersedes the candidate's inline account span; both would print the account twice) |
| `features/agent/composer.tsx` | canonical v9 (account rows follow the Bloom selection) + candidate: `submitDisabled` (credit limit), no send with an unqualified model, opt-in wording "Allow public quotes from my own writing" |
| `features/agent/conversation-view.tsx` | candidate (credits, submission gate, strict account previews, workspace keying) + canonical typed tab state, per-platform chips and `accountLabel` |
| `features/agent/home-view.tsx` | candidate (brief save, credit limit, URL run recovery, gate, public-quote opt-in default off, collapsed "Campaigns & suggestions") + canonical v9 placeholder and sub-headline, minus the unconditional "in your voice" claim |
| `features/agent/home/use-home-generation.ts` | candidate (gate, idempotency key, credit quote, URL recovery, edit-base check, save lock) + canonical refreshed-draft handling and `pendingReview` report |
| `features/agent/raffi-planner.tsx` | canonical v9 + candidate: chosen weekday/time/time zone, no paid recurring writers in credit mode, "Choose a writer" guard, Snooze |
| `web/tests/rafii-evidence.cjs` | canonical (seeded principal, signed-out routes, tours, `--merge`) with its global `net::ERR_FAILED` filter replaced by per-URL accounting and a named, empty `EXPECTED_BLOCKED` list |
| `web/tests/social-learning.test.cjs` | canonical comment + candidate's call matchers |

  - Removed as dead duplicate: candidate-only `features/channels/saved-channel-folders.tsx` (imported nowhere; canonical `ChannelFoldersSection` is mounted). Backup kept.
  - Not for promotion: `web/tsconfig.json` (only `next dev` dist-dir includes), `docs/postriff-research-20260918/evidence/receipt-snapshot.json`.
- Tests on the merged candidate: `tsc --noEmit --incremental false` exit 0 (`types-after-merge-2.log`; one stale generated `.next/types` file for the deleted route was moved aside after `next typegen`); `npm run lint` exit 0, 0 warnings/0 errors (`lint-after-merge.log`); Node 30 files / 120 tests: 119 pass, 1 fail → fixed (variable rename that broke a source contract) and re-run 2/2 (full re-run pending in the next milestone).
- Canonical promotion: not yet (FINAL-12).

## FINAL-01 — POST 500 and the 42 network findings — `LOCAL_VERIFIED`

Full record: `ROOT-CAUSE.md`.
- POST 500 = `POST /api/auth/verify` when two first-visit requests race (dev Strict Mode double effect). Cause: `Ledger.ensure_entitlement` check-then-insert (`UniqueViolation 23505 pr_entitlements_pkey`, frames `launch_credit_fixture.py:45` → `billing.py:59`). Also reachable in production (`usage_view`, legacy `reserve`). Fix: `ON CONFLICT(workspace_id) DO NOTHING` + re-read. RED 24/24 races fail → GREEN 0 (`tests/phase2/postgres_entitlement_race.py`).
- 42 × `ERR_FAILED` = `https://va.vercel-scripts.com/v1/script.debug.js` (Vercel Analytics debug script) aborted by the test's own off-origin rule. Fix: render `<Analytics />` only when `VERCEL=1`.
- Browser GREEN on a fresh environment (runs 2 and 3): 0 aborted, 0 failed, 0 HTTP ≥ 400, 0 console errors across `/app`, `/app/channels`, `/app/account/billing`.
- Snapshot of these checks: `billing.py` 4c4446a1…, `layout.tsx` 427d40f7…. Not yet re-run as the full 42-capture sweep (FINAL-11).

## Verified backlog from the six read-only audits (00:00–00:45)

Audit reports were produced by read-only agents against the candidate; items below are carried as findings to verify with a failing test before changing code. `C` = code-only, locally testable; `L` = needs live external service or authorization.

### FINAL-02 / FINAL-09 creation, identity, approval — `IMPLEMENTED_UNVERIFIED` overall
| # | Finding | Sev | Status |
|---|---|---|---|
| A1 | Review/approval takes the account from the client and checks only the platform (`store.py:356,369`); Queue schedule dialog offers every same-platform account without a default | High, C | LOCAL_VERIFIED (server): `build_manifest` refuses another account for an account-bound draft (409); RED→GREEN `tests/test_final_approval_accounts.py` (`approval-accounts-red/green.log`). UI: schedule dialog locks to the draft's account, `IMPLEMENTED_UNVERIFIED` until browser run |
| A2 | `quick_start` drops the client `idempotencyKey` (`ideas.py:932`); retry after timeout starts a second paid run; credit-mode `turn` authorizes before the replay lookup (409 instead of replay); same key + different payload silently returns the earlier run | High, C | LOCAL_VERIFIED (server): key bound to a request fingerprint stored with the run (`usage.request`, later writes merge), replay before any credit authorization, 409 `idempotency_conflict` for a different request, quick-start replay returns the original conversation/source; RED→GREEN `tests/phase2/postgres_final_idempotency.py` (5 checks incl. crash after reservation). Client: keyed resend on lost response (network, 150 s timeout, 502/503/504 without app code), never on an app answer; `web/tests/credit-turn.test.cjs` 11/11 |
| A3 | Channels named in the brief override the Home selection (`intent.py:307-348`); Home can hide drafts that Save persists | Med-high, C | LOCAL_VERIFIED (unit): intended behaviour kept ("channels you name win"), but every destination the run wrote is now listed after the chosen ones (`home/generation-items.ts`, `web/tests/home-generation-items.test.cjs`) |
| A4 | URL recovery loses the saved edit of a refreshed draft (edit clears `proposedUpdate`) | Med, C | LOCAL_VERIFIED: server keeps `runRefs` on refreshed drafts (`tests/phase2/postgres_final_run_refs.py`); UI resolves created → pending proposal → edited refresh (`savedTextFor`, unit test) |
| A5 | Run polling stops silently on the first error; no Unknown state; no client timeout; 502 returns no run id | Med, C | IMPLEMENTED_UNVERIFIED: polling keeps retrying with backoff (≤20 failures, cap 15 s); draft requests time out at 150 s and are resent with their key; exhausted resends say the request may still be running. Unknown-state UI not done |
| A6 | Plan approval has no synchronous lock; brief/destination storage not cleared at sign-out | Low, C | IMPLEMENTED_UNVERIFIED: ref lock on approve/save in `plan-card.tsx`; sign-out clears `rafii.brief.*` and `rafii.destinations.*` (browser check pending) |

### FINAL-04 providers — `LOCAL_VERIFIED` for the code paths below (synthetic transports); live gateway `NOT_RUN`
Sources: AI Gateway docs (provider filtering and routing 2026-09-10/11; image generation 2026-09-08). `providerOptions.gateway.only` restricts routing *and fallbacks*; `order` is only a preference; top-level `models` means model fallbacks.
- Writing (Chat Completions): every request carries `providerOptions.gateway.only` = the configured list for that model (`POSTRIFF_MODEL_PROVIDERS` JSON) or, by default, the model maker's own slug; no `models`/`provider` fallback fields. Serving provider (`gateway.routing.finalProvider`) and `gateway.cost` are read from `providerMetadata` or `provider_metadata` (shared `model_runtime.gateway_routing`); gateway cost is preferred for settlement. An answer served outside the list is refused as a known-cost failure. `finish_reason: length` → warning + one retry, then a clear failure with the known cost. Cancel is checked before a retry and before the deep revise pass. A cost computed from tokens now records `priceBasis` (`configured` or `defaults-2026-09-23` + the per-M prices used). `tests/test_final_gateway_routing.py` (6), `tests/test_final_runtime_failures.py` (7).
- Images (`/v1/images/generations`): same `only` restriction (docs show `providerOptions` accepted in that body; that the images route honours `gateway.only` is **not verified live**); cost read from `usage.cost`, else gateway metadata, so a normal image no longer waits for reconciliation when only metadata carries the cost; an image made outside the list is not kept, the media credit is given back and the known cost booked (`release`/`released`), unknown cost stays for reconciliation. RED→GREEN `tests/test_final_image_routing.py` (4; `image-routing-red.log` → `final04-runtime-green.log`), `tests/phase2/postgres_final_image_routing.py` (2 checks, exit 0; no DB RED run).
- Preference-learning extraction (background, `anthropic/claude-haiku-4.5` through the same gateway when no CLI is on the API host) previously sent no provider restriction: it now carries the same `only` list, reads the gateway cost (exact Decimal rounding instead of float `ceil`) and discards an answer served outside the list (settles as unknown for reconciliation, like any extraction failure). One `model_runtime.provider_map` now parses `POSTRIFF_MODEL_PROVIDERS` for drafting, images and learning. RED→GREEN `tests/test_final_learning_routing.py` (4; `learning-routing-red.log` → `learning-routing-green.log`, 48 runtime tests incl. existing learning-model tests).
- No CLI for normal users: the Models page now lists PostRiff writers before "Local CLI"; the CLI install steps show only when the API reports a local deployment (`hosted === false`); on a hosted deployment (`VERCEL=1`) or when unknown they sit in a closed "Advanced: use your own CLI on a self-hosted PostRiff" disclosure. The model dialog shows its API/CLI switch only when the API host reports a CLI model or agent; the empty catalog says no writer is set up rather than asking the person to install one. Tour steps follow the new order. Browser check on 4462 (dev harness, local): order `models-current → models-managed → models-cli → models-empty`, steps visible because the harness is local. tsc 0, lint 0/0.
- Reviewed, unchanged: Home's "Use this text to draft with" starts checked in canonical v9 as well; Generate is disabled while it is unchecked and the server refuses a quick start without `confirmUse: true`, so the constant in the payload is not a bypass.
- Blockers (L): no live gateway call has been made (needs an approved paid test with a spending cap). Budgets default to `candidate` and paid reservations need `approved`; only the local fixture approves, so paid drafting answers 402 until an operator approves a budget — production budget rows not inspected (FINAL-AUTHORIZATION). Default price table not checked against the gateway price list. Credit mode for voice analysis, images and learning extraction not reviewed end to end (FINAL-05 gap).

### FINAL-05 credits — `IMPLEMENTED_UNVERIFIED` (core items LOCAL_VERIFIED)
Done (each RED→GREEN on disposable PostgreSQL unless noted):
- Failure settlement by proof (`model_runtime.ProviderFailure` with `dispatched` + known `cost_usd`; dispatch tracked per call): refused before any provider request → hold released, specific reason shown; 429 = known zero-cost refusal with 1.5 s backoff; 4xx rejection → not charged, provider cost booked; 5xx/no answer → unknown. `tests/test_final_runtime_failures.py` (6), `tests/phase2/postgres_final_unknown_usage.py` (RED recorded against the pre-change files, `unknown-usage-red.log`).
- Unknown-usage reconciliation: `Ledger.unknown_reservations` / `Ledger.reconcile_unknown` + operator tool `scripts/reconcile_unknown_usage.py` (loopback unless `--confirm-host`; evidence + operator required; once per reservation; audit event `usage.reconciled`). `failed` never charges, `completed` charges actual within the approved limit.
- Provider cost → micro-USD rounded up exactly (`ideas.usd_micro`, Decimal) at every settle site (was truncating `int()`).
- Server estimate before approval: `POST /ideas/credit-estimates` (`CreditRequests.estimate`) from the same projected request the writer receives (`IdeasService._project`, shared by `turn`; `estimate_request` mirrors quick-start's source step via `_quick_start_source`); returns a labelled single-attempt estimate + the conservative ceiling; quote issuance refuses a limit below the ceiling with the ceiling named. UI: `CreditLimitField` shows "Usually about Y · up to X held · Use X" on Home and follow-ups (`useCreditEstimate`, debounced). `tests/phase2/postgres_final_credit_estimate.py` (4 checks; synthetic price 3/15 per M: estimate 17.7 vs ceiling 117.2 credits).
- Quick-start now sends the typed thought as the writer's `idea` (was the workspace's older saved brief); idea bounded to 3,000 chars (a 3,000–6,000-char follow-up previously failed before dispatch).
- Deep-mode reservation ceiling (found while building the FINAL-10 cost table): the revise pass was bounded by the 1 MB transport cap counted as tokens, so a deep 2-destination Sonnet draft held $2.19 / 657.0 credits (a new workspace has 50). It is now bounded by 2 × `max_tokens` (every call sends `max_tokens`=2,400; the revise pass re-reads the first draft): $0.10 / 30.7 credits, still ≥ the worst case of 3 calls at full output. RED→GREEN `tests/test_final_deep_ceiling.py` (2; `deep-ceiling-red.log` → `deep-ceiling-green.log`, 28 runtime tests).
Audit gaps since closed (above and FINAL-08): server estimate before approval, unknown-usage reconciliation, exact USD rounding, campaign worker credit authority.
Remaining gaps: voice analysis rounds with `ceil` (different from `usd_micro`); credit mode for voice analysis, images and learning extraction not reviewed end to end; the kill switch (`POSTRIFF_CREDITS_ENABLED=0` after use) falls back to the legacy view instead of explaining; 10,000-row read cap; `greatest()` clamps on legacy media counts; no Unknown-state UI (A5).

### FINAL-06 top-up / webhook / refund / dispute — `LOCAL_VERIFIED` for the items below (synthetic signed events; Stripe Sandbox `NOT_RUN`)
Done (RED→GREEN on disposable PostgreSQL; migration `022_credit_payment_lifecycle.sql`, disabled-by-default like 021):
- Durable review inbox `pr_credit_payment_inbox`: a verified credit event that cannot be applied (unknown order, amount/workspace mismatch, unrecognised status, same-second contradictory refund or dispute) is stored as `needs_review` with a reason and a minimal PII-free record, acknowledged (2xx, replay → duplicate), nothing granted/reversed; outages (5xx) still fail for retry; without 022 the old fail-closed behaviour remains. Operator tool `scripts/credit_payment_inbox.py list|resolve` (resolving never moves money).
- Order end states `expired`/`failed` from `checkout.session.expired` / `checkout.session.async_payment_failed`; a repeat of the same request never gets an ended or paid session URL back (409 / `status: funded`); Checkout sessions expire after 1 h; a verified payment still funds an order already marked expired (money wins).
- Funding also checks the session's `client_reference_id` workspace against the order.
- Disputes (`charge.dispute.created|updated|closed|funds_withdrawn|funds_reinstated`): withdrawn funds reverse the order's credits, won/reinstated restores, inquiries (`warning_*`) reverse nothing, lost stays reversed (`pr_credit_disputes`).
- Debt repayment: debt left by a take-back after spending is repaid from the earliest-expiring usable credits in the projection; only unpaid debt blocks spending.
- Live/test key mode fix (above). Subscription owner emails now sent after commit (RED: email sent while rows locked → GREEN; `tests/phase2/postgres_final_billing_notice_locks.py`).
- Tests: `tests/phase2/postgres_final_purchase_lifecycle.py` (7 checks), plus existing `postgres_credit_purchases.py`, `postgres_billing_stripe.py`, `postgres_billing.py` re-run exit 0.
Original audit findings kept for context:
Owner-only, server-priced, one grant per payment under replay/concurrency. FIXED: live/test mode now derived from all four key prefixes (`rk_live_` = live), unknown formats refused (503), and every webhook incl. subscriptions must carry a matching `livemode` (RED→GREEN `tests/test_final_stripe_mode.py`; fixtures updated to include `livemode`). Gaps: no durable webhook inbox; orders only `pending|funded` (no expired/failed; `async_payment_failed`, `checkout.session.expired` unhandled; a repeat returns a paid/expired URL); funding does not check workspace/price; refund ordering by created-second (same-second 409 loop); no `charge.refund*`; no disputes; debt cannot be repaid; subscription path never checks live mode; subscription owner email sent while holding row locks; new tables force RLS without policies.

### FINAL-07 monthly subscription credits — `LOCAL_VERIFIED` for the candidate policy (synthetic; plans not activated; commercial terms `NOT approved`)
- `invoice.paid` (verified) with `billing_reason` `subscription_create|subscription_cycle` on plan terms carrying `creditPolicy` + `monthlyCredits` grants that amount once per invoice id (`subscription-invoice:<id>`), expiring at the period end (no rollover); stored in `pr_credit_subscription_grants` with subscription, period, amount, currency, payment intent, plan terms. Checkout completion, subscription updates, failed payments and proration invoices grant nothing (proration recorded with a note: policy pending). Out-of-order delivery: status update marked stale, credits still granted. Top-up lots never reset/expired by the cycle; expiring monthly credits are spent first. Refund/dispute of a plan payment takes back that period's credits proportionally. Legacy plans unchanged.
- Stripe invoice parsing reads invoice id, billing_reason, period start/end, amount_paid, currency and payment intent in both the older and 2025+ (`payments`) shapes — the 2025+ shape is unverified against live Stripe.
- `tests/phase2/postgres_final_monthly_credits.py` (8 checks).
- Still open: upgrade/downgrade credit proration policy, trial credits, rollover, refund of consumed monthly credits (creates debt repaid from later grants — policy to confirm), plan change resolved by stale metadata plan id, Stripe API version not pinned.

### FINAL-08 six features — `IMPLEMENTED_UNVERIFIED` (partial; items below LOCAL_VERIFIED)
Done:
- Campaign: a new recurring task stores a chosen destination (platform or one connected account), language and voice mode inside its definition digest; the worker drafts for exactly that; words inside campaign data no longer redirect platform/language/time or turn the run into a memory instruction; unknown accounts refused; tasks without a choice keep LinkedIn + neutral (no silent upgrade). Planner UI: Draft for / Language / Voice selects. Worker credit checks now use the worker-bound repository — RED proven with production-like strict verification (every occurrence held when credits enabled) → GREEN (`tests/phase2/postgres_final_campaign_destinations.py`, `campaign-credit-authority-red.log`).
- Suggestions: new evidence rules `upcoming_event` (confirmed campaign date ≤14 days, no draft; not duplicated as campaign_gap) and `missing_variant` (current brief drafted for some ready accounts but not another); `suggestionsCheckedAt` recorded and shown; "Not checked yet" vs "No suggestions" empty states; copy says refresh is manual, not background; 7-day dismiss cooldown; accurate unused-image wording (`tests/test_final_suggestions.py`, 4 + 5 existing).
- Truthful copy: analytics/insights no longer claim automatic readings (production worker does not read insights yet); OAuth analytics explanations say what the permission allows; Inbox says comments are read once at verification; voice approval says drafts use it when "Writing like me" is chosen.
Remaining (not approved to drop): user-defined calendar-gap suggestions need a cadence setting; scheduled insight re-reads (needs live provider access to validate); campaign items/assets/slots editing and per-item review links; per-rule voice editing; Brand Brain facts with validity; Inbox polling beyond Threads.
Original audit notes:
Campaign: fixed LinkedIn (`campaigns.py:128`), always `en`, `voiceMode: neutral` (`campaign_worker.py:104`), no destination/language/voice/items editing, items link only to a conversation, one occurrence per cron minute, missed >24h not tested, revoked sources silently dropped. Suggestions: only `unused_asset`, `campaign_gap`, `held_draft`; manual refresh only; no last-checked; accepting creates no work object. Voice: "New drafts are written with it" misleading when neutral; profile silently dropped unless every evidence sample is granted. Analytics: production worker never reads post insights, so several copy lines are untrue in production. Inbox: Threads only, read once.

### FINAL-09 review / publish reliability — `LOCAL_VERIFIED` (synthetic providers); live publishing `BLOCKED_EXTERNAL`
- Matrix and acceptance package: `FINAL-CAPABILITY-MATRIX.md` (operations × runtime; account × operation × format with official constraints re-checked 2026-09-24; exact-approval field list; worker-state evidence; live test proposal awaiting authorization).
- Exact approval already binds every field the prompt lists (variant + content revision, account + provider account id, language, source and voice digests/revisions, text, media hashes/alt/rights, resolved time + zone + fold, root digest); changes force a new review; another draft on the same platform is never a fallback (FINAL-02 A1). Worker states pending/scheduled/processing/failed/uncertain/verified, crash before/after submit and reconcile-before-retry are covered by existing tests (listed in the matrix §3).
- Fixed from the official-docs check: (1) LinkedIn commentary is `little` text — the approved caption was sent raw, so `(`, `@`, `[`, `*`, `_` … could be read as markup and the post differ from the approval; now escaped (`provider_candidates.little_text`), word hashtags kept, read-back and LinkedIn history compare/import the plain rendering (`little_plain`). RED→GREEN `tests/test_final_linkedin_little_text.py` (2) + provider tests (26, `little-text-*.log`). (2) Threads fails posts with more than 5 links: a review is refused up front with the reason (RED→GREEN `tests/test_final_threads_links.py`, 35 approval tests, `threads-links-*.log`).
- Checked and fine: LinkedIn version header `202609` (supported to 2027-09-15); Instagram receives a JPEG rendition of every upload.
- Gaps: Threads counts emojis as UTF-8 bytes (our 500 limit counts characters); Instagram width >1440 and the 50-vs-100 daily limit unverified; escaped LinkedIn text can be longer than the 3,000 checked on raw text; LinkedIn image posts not implemented in the hosted worker.

### FINAL-10 security / ops / legal — `IMPLEMENTED_UNVERIFIED` (partial; items below LOCAL_VERIFIED)
Added 2026-09-24 (evidence files written 07:40–07:46Z):
- Test conveniences are not production backdoors (verified by reading): all six `/app/dev/*` pages call `notFound()` when `NODE_ENV=production`; the proxy's dev-cookie bypass works only without Supabase env; the production entry (`api/index.py` → `runtime_from_environment`) builds only the Supabase verifier, and the dev verifier lives in `scripts/postriff_dev_hosted.py`, which the release staging list excludes; production passes Stripe or `DisabledPaymentProvider`. Defence in depth: the fixture payment provider (published test secret) now refuses every webhook when `VERCEL=1` (RED→GREEN `tests/test_final_fixture_guard.py`, `fixture-guard-*.log`).
- Report-only secret scan of the candidate (2,502 text files, 266 binary; values never printed): `.env.example` placeholders, synthetic DSNs in deployment tests (identical to canonical) and fictional fixture archives; no live credential found (`secret-scan-summary.json`). Canonical to be scanned again after promotion.
- Operator view: `scripts/ops_health_report.py` (read-only connection, loopback unless `--confirm-host`) lists unknown usage, payment events awaiting review, uncertain/held jobs, expired `processing`/`submitting` leases, `scheduled` jobs >15 min overdue and budgets past warning/stop or still `candidate`, identifiers only (`tests/phase2/postgres_final_ops_report.py` exit 0). Recovery steps are in FINAL-DELIVERY (runbook).
- Cost table (formula estimates, p50/p95 not measured, default price table unverified): `evidence/final/r1/cost-estimates.json` — Sonnet 5: caption typical 1.8 / ceiling 18.3 credits; 4 destinations 5.6 / 19.1; deep 2 destinations 6.1 / 30.7. Haiku 4.5 about half. Image estimate $0.10 (30 credits); research unmetered; hosting, storage, payment fees, failed/unknown runs and support not included.
- Legal facts still open (not filled in; `LEGAL_REVIEW_STATUS='draft'` banner stays): merchant entity and address; governing law (`[Jurisdiction — to be confirmed by counsel]`); privacy/support mailbox (`privacy@postriff.app` marked TODO); data regions for Vercel, Supabase, Resend, AI Gateway route, Exa, Jina; log (30 days) and backup (30-day) retention are "candidate"; post-metrics retention pending; Terms §6 covers only the trial — credit packs (expiry, refunds, disputes), subscription renewal/cancellation and monthly-credit policy are not written.
Done and tested earlier: `account_pictures.fetch_image` TLS context moved into an `HTTPSHandler` (RED TypeError → GREEN, `tests/test_final_account_pictures.py`, verification stays CERT_REQUIRED); 500 log line now carries `exceptionType` + `routePattern` with identifiers masked, never exception text (`tests/test_consumer_request_logs.py`); `web/.gitignore` ignores every `/.next-*/` dist dir. Remaining below.
No dev/test backdoor reaches production (verified by reading). Blockers: legal facts (entity, governing law, refund policy, mailboxes, regions, transfer mechanism, credit-pack terms — owner/counsel, L); unknown-usage settlement; no log handler (INFO dropped, exceptions swallowed without type); `account_pictures.fetch_image` passes an unsupported `context` argument → TypeError → 500 after a channel is saved (C); no rate limits on drafting/research/uploads; default billing provider falls back to the fixture provider with a published secret when none is passed; no CSP/HSTS; `web/.next-finish` (924 MB) and `web/.next-model-selector` not git-ignored.

### FINAL-03 copy — `LOCAL_VERIFIED` for the P0 pass below (same viewport/state before-after in the browser); settings/marketing P1 pass not done
Measured on the harness at 1440×1000 with the same script before and after (`evidence/final/r1/final03-copy-measure.json`, method and limits inside). Persistent words / visible controls:
| Page | Before | After | What moved or changed, and where it went |
|---|---|---|---|
| Home | 868 / 57 | 861 / 57 | Preview caption drops a duplicate "nothing is published"; the approval line under Generate stays |
| Conversation | 341 / 39 | 360 / 39 | Raw reason codes (`egress_consent_required`) now say why and where to change it (longer, clearer); a source the live run already warned about is listed once |
| Channels | 431 / 53 | 351 / 54 | Unconfigured provider: one visible reason; env names, callback URL and credentials note in a keyboard-operable "Setup details for whoever runs this PostRiff" disclosure (+1 control); duplicate capability/"App configuration" sentences removed (page description keeps the point); companion paragraph shortened, keeps "never from our servers" and "Not available yet" |
| Brand | 611 / 42 | 600 / 42 | Duplicate "review and schedule before setting this up" removed (status card keeps it); picker keeps "never starts AI analysis", drops a duplicate publishing guarantee; every consent checkbox, egress line and AI-review label unchanged |
| Queue | 112 / 16 | 109 / 15 | First-run block shows one next step (Schedule when drafts exist, otherwise Ideas); header action unchanged |
| Billing | 238 / 18 | unchanged | Every line is cost, payment confirmation or settlement state |
Checks from the prompt: Brand already says AI suggestions need approval and labels AI analysis "needs human review" (no "nothing is model-inferred" claim remains); Models no longer says a paid writer is substituted (see FINAL-04); unsupported analytics readings render "Unavailable", never 0 (`metric-value.tsx`); Channels shows capability levels separately. The Home content library (11 cards with "Usually:" lines, the largest persistent block) and the NotificationStack were left as v9 designed them — shortening them changes the v9 look and is an owner decision, not a safety fix. tsc 0, lint 0/0.
### FINAL-11 same-snapshot regression + browser matrix — `LOCAL_VERIFIED` on release snapshot r6
- r2 (snapshot `038bfd82…`, 1,180 files): Python 669 OK, tsc 0, lint 0/0, Node 31 files / 127 pass, DB suite 46/47 — `postgres_ideas.py` asserted the raw code `internal_reference` inside the warning text that FINAL-03 made readable. Fix: the warning event keeps the code in a `reason` field; the assertion now checks the code *and* the readable sentence (stricter, not weaker).
- Superseded by the FINAL-12 sync below (canonical changed during this work), so the gates were re-run: r4 (`b00dc6a4…`, 1,190 files): Python 689 OK, tsc 0, lint 0/0, DB 47/47, Node 134/135 — candidate-only contract `launch-wiring.test.cjs` still looked for the old Home weekday/time selects; scheduling moved to canonical's Automation builder, so the contract now checks the builder's weekday toggles, time and zone fields and the planner's snooze (same intent, new location). 
- r5 (`76a6326c…`): Python 689 OK, tsc 0, lint 0/0, Node 135/135, DB 47/47. Production build: the first attempt failed because the candidate's `web/node_modules` is a symlink to canonical's and Turbopack refuses a symlink outside the project root (environment, not code; bundler not switched). Built like CI instead (`consumer_ready_web.py`'s rules: an isolated copy without `.env*`/`.next*`, sanitized env, `npm run build`) with an APFS clone of canonical's `node_modules` (same lockfile): Next.js 16.3.5 Turbopack, exit 0, no warnings (no Google Sans Flex override message). Browser on that build (API 4438 harness + web 4439, system Chrome): both journeys' functional steps passed but each logged React #418 on Home; dev-mode diff pointed at the app shell (server placeholder vs client user in the account menu); the same error reproduced on canonical's own build without this integration (`r5/browser/hydration-diag-canon.json`), so it was not introduced here. The legacy journey also still drove the old Home campaign form: its schedule step now builds a draft automation in the Automation builder (same assertions: Thursday 20:45, status `draft`, snooze persists), and the brief-restore check waits for the restored value instead of reading at load (the value is restored after hydration).
- Canonical fixed the #418 while this ran (`a4c1a80`, account menu renders the placeholder until hydrated); synced into the candidate with `rafii-automations.cjs` (`r3/sync-2-log.json`).
- **r6 = release snapshot** `d9573b5e1635d406ccd78a00eff5f451beb5cbdc0dbeb36af6a167e7550f8016` (1,190 files, frozen 08:37:35Z; identical after all runs): Python 689 tests OK, 0 skipped (27.3 s); tsc exit 0; lint 0 warnings / 0 errors; Node 31 files / 135 tests, 135 pass, 0 fail, 0 skipped; PostgreSQL 47/47 scripts exit 0, source unchanged; production build exit 0 (Turbopack, 12.4 s compile, no warnings). Browser (production build, system Chrome, synthetic identities): legacy journey exit 0 (4 steps, 0 page errors, 0 failed requests, 0 critical/serious axe); credit journey exit 0 (6 steps: quote/reserve/settle 50→47, follow-up 47→44, top-up 1,000 credits once and never before payment; 0 page errors); route matrix 44 routes × 390/768/1024/1440 × light/dark = 352 captures, 0 horizontal overflow, 0 axe violations (axe on all 352), remaining errors all named: 16 × 500 on `/app/account/api` (the harness database lacks migration `016_api_tokens`; known since the v9 evidence), 8 × 404 on the not-found route (expected), 6,295 × `net::ERR_ABORTED` that are Next.js router prefetches (`fetch`, `_rsc`, `next-router-prefetch: 1`; `r5/browser/aborted-diag.cjs`) while every full navigation succeeded. Logs: `evidence/final/r6/`.
- Not covered: WebKit, 200% zoom, slow network, real phones, keyboard-only walkthrough beyond the scripted flows (the route matrix is Chromium only).

### FINAL-12 review, promotion, delivery — canonical working tree integrated (no git action)
- Canonical moved on during this work: branch `rafii-v9-integration` (pushed by another session; tip `62c64ae`, 03:54 EDT) added the Automations hub (`b49b9f1`, `10c9459`, `a897ec5`), Analytics opt-in and deferred dialogs (`503ab56`), a Google Sans Flex fallback fix (`132b017`) and CI; the canonical working tree carries that code. Read-only plan (`evidence/final/r3/promotion-plan-pre.json`), base = canonical content at FINAL-00 (r1 hashes): 119 files changed only by this integration, 19 changed only by canonical since FINAL-00 (10 new), 9 changed by both.
- Sync into the candidate (backups `evidence/final/r3/pre-sync/`, log `sync-log.json`): the 19 canonical-only files taken verbatim; the 9 both-changed files merged three-way with the FINAL-00 base recovered from branch history (commits `fdc090d`, `88088e7`, `092f49d`, `fceeec6`, `b0b719d`; hashes checked): 4 clean (`hosted_app.py`, `home-view.tsx`, `types.ts`, `rafii-evidence.cjs`); `layout.tsx` → canonical's opt-in (supersedes ours, same root cause); `campaign_worker.py`, `campaigns.py` → canonical's automation model (multi-destination v2, `definition_digest`, always-neutral voice) keeping ours: v1 destination choice, unknown-account refusal, worker credit authority; the per-task `voiceMode` choice was dropped because canonical's worker never uses it (test updated to check the drafted voice is neutral); `ideas.py` → canonical's stricter automation neutralisation plus its automation content-type selection and notes, ported into the shared `_project()` so estimates see the same request; `raffi-planner.tsx` → canonical's Automations strip plus ours: "last checked"/"Not checked yet", missing-variant route, Snooze. Verified every line canonical added since FINAL-00 is present in all 9 merged files (0 missing); ours missing only where deliberately superseded.
- Promotion 2026-09-24 08:54:30Z (`evidence/final/promote_to_canonical.py apply`; manifest `FINAL-MERGE-MANIFEST.json`, copied to canonical): 128 files — 54 integration changes, 8 three-way merges (the ninth, `layout.tsx`, already equals canonical), 66 new files — each written only after canonical's current content matched the recorded pre-image; 0 skipped, 0 blocked; canonical pre-images saved in `evidence/final/promotion/canonical-pre-images/`; 6 deliverable docs copied. Excluded: `web/tsconfig.json` (dev dist-dir includes), evidence folders. Nothing staged, committed, pushed or merged; the index is untouched. Rollback: `FINAL-ROLLBACK.md`.

## Test runs on the working candidate (not a release snapshot)
- 2026-09-24 04:59Z Python `unittest discover`: 636 tests, exit 0, 0 skipped (`evidence/final/r1/python-mid.log`; 2 pre-existing CLI ResourceWarnings).
- 05:23Z Python: 642 tests, exit 0 (`python-mid2.log`). Node: 30 files / 124 tests, 0 fail/skip (`node-all-mid.log`). Lint 0/0 after one fix. tsc exit 0.
- 05:24–05:34Z full PostgreSQL suite (`db-suite/summary.json`): 38/40 scripts exit 0, `sourceUnchanged: true`; the 2 failures (`postgres_credits.py`, `postgres_final_idempotency.py`) were a regression in the new estimate (it re-created an existing source) → fixed with the shared `_quick_start_source`; both + `postgres_final_credit_estimate.py` + `postgres_quick_start_again.py` re-run exit 0 (`*-rerun.log`). Full suite to be re-run on the release snapshot (FINAL-11).

## Next executable step
Owner review of `FINAL-AUTHORIZATION.md`, then (only with that approval) commit the promoted files on a branch by path and run item 1 (live model qualification, cap US$3). Superseded history: the step recorded here earlier (FINAL-05..07) is done.
