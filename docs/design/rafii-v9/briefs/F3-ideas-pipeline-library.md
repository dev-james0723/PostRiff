# Stream F3 — Ideas (sources), Pipeline (drafts) and Library (assets)

Common brief: `F-page-migration-common.md`. Recipes: DNA §21.10 (Sources/References), §21.2 (Draft editor), §21.9 (Assets/Media library); prompt §10 rows "Home/Create and draft detail", "Voice/Brand Brain/Sources/Assets".

Files: `web/src/features/ideas/**`, `web/src/features/pipeline/**`, `web/src/features/library/**`, `web/src/app/app/ideas/page.tsx`, `web/src/app/app/pipeline/page.tsx`, `web/src/app/app/library/page.tsx`, `web/src/components/application/asset-picker.tsx`. Sources: document rows with type/title/origin/inclusion/policy; keep "include in analysis" and "permission to quote publicly" as separate, explicit controls (never one vague checkbox; never pre-enable public quoting); failed fetch ≠ empty; retry/removal per source. Pipeline: stable draft identity row, readable editor text (regular 15–16px, not italic display), per-channel edits distinct from shared input, one primary action by state (Save changes / Request review / Schedule), set-aside/detail sheets as elevated glass. Library: Gallery/List through `Workbar` + `SegmentedControl` (only if both already exist — do not add a mode), real images in colour, upload/processing/failed/ready states distinct, licence/rights inspectable, selection count + stable confirm in pickers.
