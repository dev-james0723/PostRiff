# Stream F2 — Inbox and Channels (connections)

Common brief: `F-page-migration-common.md`. Recipes: DNA §21.5 (Inbox/Conversations) and §21.6 (Channels/Connections); prompt §10 rows "Inbox/conversations" and "Channels/connections".

Files: `web/src/features/inbox/**`, `web/src/features/channels/channels-view.tsx`, `channel-card.tsx`, `connect-sheet.tsx` and any other file directly under `web/src/features/channels/` EXCEPT the folder `web/src/features/channels/channel-bloom/` (another stream owns it — do not create or edit anything there), `web/src/app/app/inbox/page.tsx`, `web/src/app/app/channels/page.tsx`, `web/src/app/app/channels/connect/page.tsx`, `web/src/app/channels/connect/page.tsx` (OAuth return/forwarder: style only its Rafii-owned entry/return/error surfaces; never change the exchange logic, query handling or redirects). Inbox: readable list/thread hierarchy, real unread/reply states, mobile list→thread steps with Back, preserved reply drafts, reply grant/permission states shown honestly. Channels: account identity first (platform mark + account + connection state + capabilities), connected/expired/disconnected/missing-permission/unknown states distinct, reconnect/verify/manage secondary, "Connect channel" primary, providers' setup issues kept visible. Leave a clearly commented slot in the channels view (a `{/* rafii-v9: folders */}` comment where a "Saved folders" section will be mounted later) but do not implement folders.
