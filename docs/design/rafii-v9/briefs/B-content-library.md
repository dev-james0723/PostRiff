# Stream B — Content Library (31 editorial types, 20 native formats)

Read first: `docs/design/rafii-v9/contracts.md`; DNA v8 §3.1–3.3, §13, §14, §6.3 (readable text), §12.3–12.4, §22.3; prompt §6. Prototype: extract `docs/design/reference/rafii-v9/rafii-prototype-v9-source.zip` → study `src/taxonomy-data.json`, `src/library-data.json` (categories, tags, pairings, platforms, evidence), `src/assets/thumbnails/*.svg` (51 large 8:5 illustrations), `src/assets/glyphs/*.svg` (51 compact glyphs), `src/library-v7.css`, `src/library-v8.css`, `src/app.js` (`renderFormatDeck`, `taxonomyCard`, `pairingCard`, `openLibraryTip`, `showLibraryEvidence`, `swapLibrary`, filter layer), `src/index.html` (`#format-dialog`). Backend truth: `src/postriff_phase2/content_types.py` (`CORE_TYPES`, `CREATOR_TYPES` ids like `postriff:update`, `pack.creator:*`; `FORMATS` ten format ids) and `web/src/config/quick-starts.ts` (existing mapping of 11 templates to backend types/formats).

## Files you own
- `web/src/lib/content-library/` (new): `taxonomy.ts` (the 51 items with title, description, family, dimension, category, tags, platforms fit, pairings, evidence refs — ported losslessly from the JSON with a build note), `registry.ts` (stable ids → `{ contentTypeId?: string; formatId?: string; execution: 'mapped' | 'planning-only'; note }` mapping every editorial type onto a real backend content type where the meaning matches (quick starts show the pattern) and every native format onto one of the ten backend formats or `planning-only`), `artwork.ts` (imports of the SVG assets with metadata: `thumbnail_id`, `alt_text`, `aspect_ratio`, `content_hash`, `version`, `source_type`), `index.ts`, plus `taxonomy.test.cjs` (51 unique ids, 31/20 split, every item has both assets, every mapping target exists in the known backend lists, no two items share artwork).
- `web/public/rafii/library/thumbnails/*.svg` and `web/public/rafii/library/glyphs/*.svg` (copied assets; keep bytes identical; add `PROVENANCE.md` there quoting the source archive + hashes) — OR inline them as React components under `web/src/components/rafii/illustrations/` if you prefer unique-id namespacing at render time. Either way IDs (gradients/masks/clips) must be unique when the same asset appears twice on a page (DNA §14.4): implement `SemanticIllustration` (`web/src/components/rafii/semantic-illustration.tsx`) that renders an asset by id at `size='large' | 'compact'`, with `alt` (or decorative), grayscale paper treatment, and decorative motion (`glyph breathing 4.8s`, `paper 6s`, `accent opacity 4.8s`, ≤1.2px, `.rafii-decorative-motion`, paused when offscreen via IntersectionObserver, in background tabs, when `data-motion='reduced'`, or when the person pauses artwork).
- `web/src/features/agent/content-library-dialog.tsx` (new): `ContentLibraryDialog({ open, onOpenChange, value: { editorialId, nativeId }, onApply(value), platformsForFit?: string[] })` built with `RafiiDialogContent size='lg'` and the shared `Workbar`/`SegmentedControl`/`FilterPanel`/`InfoTip`/`StateMessage`.
- `web/src/features/agent/content-choice.ts` (new): helper that turns the applied library value into the workspace content selection the backend understands (`p2_content_select { contentTypeId, formatId }` after ensuring `pack.creator` is installed via `p2_content_install_pack` — see `selectContentType` in `home-view.tsx`), returning a `planningOnly` note for unmapped native formats so the composer can display it honestly.

## Behaviour to deliver
1. Header: eyebrow "Content Library", title "Find your <em>next idea.</em>", close. WHAT: two large task tabs (`SegmentedControl pattern='tabs'`, lens 400ms) "What you want to say — Editorial type · 31" and "How you present it — Native format · 20". FIND/VIEW row via `Workbar`: search (16px on phones, full-width row ≤50rem), view switch Gallery/List/Pairings (quieter, lens 370ms), Filters trigger with count; active filter summary with Clear. Filters panel: Category select (7 editorial / native groups, with counts), Suggested app fit select (all + platforms from `platformsForFit` or the taxonomy's platform list), "Artwork motion" pause/play toggle (`aria-pressed`), "About app fit & evidence" opening the evidence panel. Results meta: category title + live count "n / total".
2. Gallery: 2 columns ≥600px, 1 column below; card = large illustration (8:5) with selected check, tags (family/category), title (serif for editorial, sans for native, ≥18px), description (13–14px), "Suggested fit" platform marks (`ChannelIcon`) or "Specialist surface", and a separate `InfoTip` (never selects). List: compact glyph | title + tags | fit marks | check | info. Pairings: item heading + up to two routes (Present as / Try this intent) with why-text, evidence link (opens the evidence dialog: fit suggestion vs usage vs engagement, source, period, population, limits; research snapshot date from the JSON; no live analytics), and "Use pairing" staging both editorial and native choices with a toast "Pairing selected. Your channels are unchanged." Keyboard grid navigation (arrows/Home/End) over cards.
3. View/filter switches crossfade the collection (320ms; use `useCrossfadeSwap` or a keyed layer that keeps the outgoing DOM inert until done) and preserve the staged selection, including items hidden by the current filter; the footer shows both staged choices with compact glyphs and the note "Changes draft choices only. Channels stay unchanged. Suggested app fit does not guarantee publishing support." Primary "Use these choices" applies; close/Escape cancels (tooltip → evidence → filters → dialog order).
4. Honesty: mark `planning-only` native formats with a small label "Planning only · export or manual posting" and never coerce them; the registry's `note` explains.
5. Tests as above; typecheck + lint green. Also add a dev route `web/src/app/dev/content-library/page.tsx` that renders the whole artwork inventory (all 51 large + 51 compact) for visual audit, `notFound()` in production like `/dev/post-previews`.

Report back: file list, the mapping table (editorial → backend type, native → format or planning-only), the dialog props, limitations.
