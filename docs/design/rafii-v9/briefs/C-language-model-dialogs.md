# Stream C — Output-language dialog and Model/reasoning dialog

Read first: `docs/design/rafii-v9/contracts.md`; prompt §7 and §8; DNA v8 §15.3–15.4, §16, §10.6, §11.3, §18.4. Prototype: extract the source zip → `src/index.html` (`#language-dialog`, `#model-dialog`), `src/app.js` (`renderLanguageDialog`, `renderLocaleList`, `wireLanguages`, `renderModelDialog`, `updateReasoning`, `moveProviderLens`, `wireModels`), `src/selectors-v5.css`, `src/refinement-v6.css`, `src/refinement.css` (glass setting buttons). Production truth: `web/src/components/application/language-picker/*` (existing picker: keep `LanguageList`/`locales` search, flags, native names, aliases, regionless, Tuned badges), `web/src/lib/locales/*` (catalogue; do not fork), `web/src/features/agent/use-channel-languages.ts` (Stream A is extending selection keys to accounts; treat a selection item as `{ key, platform, channelId?, languages: LocaleTag[] | null }` and use its `languagesOf/change/add/remove/useEverywhere` API), `web/src/components/ui/model-selector.tsx` + `features/agent/use-model.ts` (real catalog: `ModelOption`, `AgentInfo`, reasoning options per model; CLI routes `claude-code`/`codex`; qualified/unqualified with `detail`), `src/postriff_phase2/cli_runtime.py` `effort()` (CLI levels low/medium/high/xhigh/max; managed/fixture routes accept quick/standard/deep; the fixture only honours quick).

## Files you own
- `web/src/features/agent/language-dialog.tsx` (new): `LanguageDialog({ open, onOpenChange, selection: LanguageSelectionItem[], languages: ChannelLanguages, accountLabel(item) })` — a staged editor over the composer's selection: "Output language for {account or platform}" target block with a `target-language-button` (flag, native, English, chevron) that opens the inline catalogue (measured-height disclosure), a list of every selected destination with its current language button(s) (multi-language per destination preserved: show each language chip and a `+` to add), and the shared box: "All channels use the same language" switch (`role='switch'`, `aria-expanded`) → the same glass box extends (measured disclosure, 480ms, interruptible; NOT grid-rows + max-height) → "Select a language for every channel" → choose → "Apply languages" enabled only once chosen. Turning the switch off restores the individual choices staged before it was turned on. Apply commits (calls `languages.change/add/remove/useEverywhere` as needed; only then are settings saved); Cancel/close/Escape restores the applied configuration. Search text and focus stay stable while the list updates. Long labels wrap; 16px inputs on phones; RTL entries use `dir`.
- `web/src/features/agent/model-dialog.tsx` (new): `ModelDialog({ open, onOpenChange, catalog: ModelCatalog, value: { model, reasoning }, onApply({ model, reasoning }) })`: eyebrow "Your creative engine", selected-model capsule (provider mark + label, animated label swap 330ms), API / CLI mode switch (segmented; CLI shows the real `agents` info: installed, version, auth status, guidance — never a fake connected badge), provider rail (`role='tablist'` vertical, one lens 440ms, providers derived from the catalog with `providerName`/`ProviderIcon` from `model-selector.tsx`; keep icon provenance: simple-icons + Tabler already bundled — do not add the prototype's Lobe icons), search over models (crossfade of the list 430ms via `useCrossfadeSwap`, direction by rail order), model rows (label, route/provider detail, availability, billing; unqualified rows disabled with the reason), and the Reasoning control: four segments Low/Medium/High/Max with the four-bar indicator (1/2/3/4 lit, bar stagger 35ms, lens 440ms) — mapped honestly to the selected model's actual `reasoning` options: CLI five levels map Low→low, Medium→medium, High→high, Max→max (xhigh reachable as "High+" only if you show all five; prefer showing the model's real list with the bars scaled to its count); managed routes with quick/standard/deep show three; a route with only `quick` shows "Not applied by this provider" and keeps the preference stored but greyed. Show under the control: "Effective for {model}: {mapped value}" / provider default. Per-model persistence: extend `use-model.ts` to remember reasoning per model id in localStorage (`postriff-agent-reasoning`) and expose `reasoningFor(modelId)`; the composer's existing `chooseReasoning` keeps working. Browsing providers/search never changes the committed model: "Use this model" applies; close cancels.
- `web/src/components/rafii/reasoning-bars.tsx` (new): the four-bar indicator, `aria-hidden`, `count` prop.
- `web/src/features/agent/setting-buttons.tsx` (new): the three glass setting buttons (Language / Model / Writing Voice) for the composer: icon, kicker, current value summary (wraps; never microscopic), `aria-haspopup='dialog'`, `aria-expanded`; export `SettingButton` and a `SettingButtons` row (3 columns, stacking to 1 column ≤374px per the prototype at narrow widths — but keep readable).
- `web/src/components/ui/model-selector.tsx` restyle only (glass trigger; keep its behaviour and the existing `web/tests/model-selector-browser.cjs` expectations: labels 'Model', 'Search models and providers', option text, 'Selected model'/'Selected effort' test hooks live on the check page `/model-selector-check` — do not break them).

## Behaviour notes
- Language dialog must keep `Output language for [account]` above individual choices; a selected folder does not change languages; changing future output settings must not translate or regenerate existing drafts (no side effects beyond `language_settings`).
- Reduced motion: disclosures snap, lenses jump, bars switch without stagger.
- Dark/light, 320px, keyboard (arrow keys on rail and reasoning segments; Escape closes catalogue before dialog), focus returns to the trigger.
- Tests: `web/src/features/agent/reasoning-map.test.cjs` for the reasoning mapping helper (put the helper in `web/src/features/agent/reasoning-map.ts`); typecheck + lint green.

Report back: file list, exact props, the reasoning mapping table, limitations.
