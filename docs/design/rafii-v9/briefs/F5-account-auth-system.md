# Stream F5 — Account settings, Auth/onboarding/invitations, system pages

Common brief: `F-page-migration-common.md`. Recipes: DNA §21.14 (Models/API config), §21.15 (Settings/Profile), §21.16 (Onboarding/Sign-in/Invitations), §21.18 (Billing/Usage), §20.4 (errors keep the design language); prompt §10 rows "Model/API/profile/workspace settings", "Auth/onboarding/invitations/public pages", "Billing/usage/notifications/error pages".

Files: `web/src/features/account/**`, `web/src/features/billing/**`, `web/src/features/onboarding/**` (welcome dialog, tour overlay/mount, help menu: keep every tour step and `data-tour` target; no forced entrance animation), `web/src/components/auth/**`, `web/src/app/app/account/*/page.tsx`, `web/src/app/auth/**` (layout + pages; the `callback/route.ts` handler is untouchable), `web/src/app/invite/[token]/page.tsx`, `web/src/app/not-found.tsx`, `web/src/app/global-error.tsx`, `web/src/components/layout/app-gate.tsx` (loading/unavailable/signed-out/dev-workspace/workspace-error states → `StateMessage` + Rafii buttons; keep the redirect logic exactly), `web/src/components/layout/info-sidebar.tsx`, `web/src/components/layout/live-island.tsx`, `web/src/components/layout/shortcuts-dialog.tsx`, `web/src/components/layout/notification-bell.tsx`, `web/src/components/layout/workspace-switcher.tsx`, `web/src/components/kbar/**` (palette as elevated glass), `web/src/components/themes/theme-selector.tsx` (label the control "Appearance"; keep all themes; Rafii is the default), `web/src/components/ui/sonner.tsx` (toasts as glass). Settings: grouped forms, masked secrets (never in toasts), saved configuration vs runtime status shown separately, one save per group or an explicit dirty bar, destructive actions in a clearly named lower section. Auth: restrained identity block, one central form, same controls as the app, errors preserve input, `next` handling untouched.
