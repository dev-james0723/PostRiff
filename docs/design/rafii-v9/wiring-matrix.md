# Control → service wiring matrix (v9 homepage, selectors and conversation)

Columns: v9 control | production component | real read/write/job action | state owner | persistence | error/cancel behaviour | verification.

Verification legend: **PW** = Playwright real-input run against the local dev harness (`web/tests/rafii-workflow.cjs`, report in `evidence/workflow/<browser>/report.json`, Chromium and WebKit); **node** = `node --test` unit/contract test; **py** = Python unit test; **pg** = disposable-PostgreSQL integration script. All drafting in automation uses the `deterministic-preview` fixture route (the script aborts any other model before it reaches the server); nothing is published.

| v9 control | production component | real action | state owner | persistence | error / cancel | verification |
|---|---|---|---|---|---|---|
| Greeting | `HomeView` header | reads `me.displayName` (`GET /api/me`) | server | — | falls back to "today?" without a name | PW screenshot `workflow/01-home-idle.png` |
| Idea textarea + Expand writing space | `IdeaComposer` (`features/agent/home/idea-composer.tsx`) + `ExpandedIdeaDialog` | local draft text; ⌘↵ submits | Home state | not autosaved (honest) | expanded and inline share one string | PW (typing, 16px on phones) |
| Context Pocket | `ContextPocket` (`home/context-pocket.tsx`) | reads active usable sources; "Add a note" uses the existing `source` action; chosen ids go to `quickStart.sourceIds` (validated server-side: active, non-voice) | Home state (included ids) + snapshot | sources on the server; inclusion per draft | refused ids → 409 before any source is stored | pg `postgres_quick_start_again.py` (source read with the idea; unknown id refused) |
| Content type | `ContentLibraryDialog` → `content-choice.ts` | `p2_content_install_pack` (once) + `p2_content_select` | snapshot (`state.contentTypes`) | server | Close discards staging; planning-only formats labelled, never coerced | node `taxonomy.test.cjs` (9) |
| Channels / folders | `ChannelBloomDialog` + `useDestinations` + `useChannelFolders` | staged selection; folders via `p2_folder_save/delete/move` | staged in dialog; committed in Home state | folders: workspace state; selection: `sessionStorage` per workspace | Done commits, Cancel/Escape discards; Undo; 409 → refetch + retry once | py `test_postriff_channel_folders`; node `folders.test.cjs`, `channel-bloom.test.cjs`; PW: overlapping folders → 2 accounts, inspector unfolds, reduced motion |
| Destinations (accounts) | `useChannelLanguages` keyed by `channelId` | `Destination.channelId` sent with the run; server `intent.bind_accounts` verifies it against this workspace's live connections | Home state → run → variants | variants carry `channelId` | unknown/revoked account → refused | py `test_postriff_destinations`; PW: server holds exactly one draft per account |
| Language | `LanguageDialog` over `useChannelLanguages` | `language_settings` on Apply (per platform memory); per-account choice for the draft | Home state + snapshot `languageSettings` | server (platform memory) | Cancel/Escape touch nothing; changing one account never re-languages another (fixed 2026-09-23) | node `language-dialog-ops.test.cjs`; PW: second account en-GB/en-AU, first unchanged, on the server |
| Model | `ModelDialog` over `useModelChoice` | choice sent with each run | localStorage `postriff-agent-model` | browser | browsing providers never changes the committed model | PW: provider swaps then Cancel keep the fixture model |
| Reasoning | `ModelDialog` segments + `reasoning-map.ts` | preference per model mapped to the route's real levels | localStorage `postriff-agent-reasoning` | browser | single-level routes show "not applied by this provider" | node `reasoning-map.test.cjs`; PW clip `motion-provider-swap-reasoning` |
| Writing Voice | `VoiceDialog` (neutral / personalized) | `voiceMode` + `voiceSourceIds` on the run | Home state | — | personalized disabled with its reason when no approved samples | manual browser |
| Generate drafts | `useHomeGeneration.start` → `api.quickStart` → `useRun` | real run; `cancelRun` | server run | server | a refused start is shown in the results column with Try again (fixed 2026-09-23); same idea again reuses its source (backend fix) | PW (2 of 2 ready); pg `postgres_quick_start_again.py`, `postgres_ideas.py` |
| Idea Splits results | `IdeaSplits` (`home/idea-splits.tsx`) + `PreviewDeck`/`PreviewDock` | `applyRun`, then `variant_edit` for captions edited here | server variants | server | an unedited refresh of an existing unscheduled draft stays a proposed update and is reported with a Pipeline link; an edited one is recorded as an author edit | PW: edit persisted as a new revision; refresh case observed in WebKit |
| Phone deck / dock / expand | `PreviewDeck`, `PreviewDock`, `ExpandedPreviewDialog`, `useSwipe` | none (preview only) | Home state | — | switching never changes destinations; rapid switches settle on the last request | node `preview-deck.test.cjs` (9); PW: swipe, dock keys, interrupts, focus return |
| Conversation composer | `Composer` (compact) | `api.turn` with the conversation's destinations | Home/conversation state | server | one chip per selected account; toggling an account removes only that account | PW conversation tabs; typecheck |
| Plan card | `PlanCard` | `approvePlan` (review → approve chain) | server | server | the row's own account is pre-selected (`channelId`), else the ready account | typecheck; not exercised (no approval in automation) |
| Theme toggle | `ThemeModeToggle` (next-themes) + Appearance (`active_theme` cookie) | — | browser | persisted | — | evidence sweep dark + light |
| Motion preference | `useMotionPreference` + `prefers-reduced-motion` | — | browser | persisted | spatial animation off, state still changes | PW `reduced-motion` scene |

## Automations (`/app/automations`, Phase 1)

| control | production component | real action | state owner | persistence | error / cancel | verification |
|---|---|---|---|---|---|---|
| New automation / Edit | `AutomationBuilder` (`features/automations/automation-builder.tsx`) | stages every step locally; Save sends one `raffi_recurrence_save` (installs `pack.creator` first when the chosen type needs it) | builder state | server (`raffi.campaignPlanning` + `pr_recurring_tasks`) | Cancel/Escape discard; refusals (disconnected account, unknown type, bad schedule) shown in the dialog; stale revision retried once | py `test_postriff_campaigns` (13); PW `rafii-automations.cjs` |
| What: brief, audience, event facts | builder step 1 | part of the save payload (campaign brief, versioned) | builder | server | event brief saves as a draft; activation waits for date and venue (reminder, not a block) | py (activation refused with the missing facts) |
| What: content type | `ContentLibraryDialog` → `contentChoice` | stored on the automation (`contentType`), not the workspace selection | builder | server | "Use general" clears it; an unavailable type drafts as general writing with a note | py (`_automation_selection`); pg (skills bound with the automation's type); PW (Home selection unchanged) |
| When: weekdays, time, zone, next runs | builder step 2 + `schedule.ts` | `schedule {weekdays, localTime, timeZone}`; preview computed like `campaigns.next_occurrence` | builder | server | incomplete schedule blocks Save with a reason | node `schedule.test.cjs` (5, incl. DST); py weekday cases |
| Where: accounts, folders, languages | `ChannelBloomDialog`, `LanguagePicker` | `destinations[{platform, language, channelId}]` + folder label (display) | builder | server | disconnected accounts must be removed before saving; at most 10 drafts a run | py refusals; pg run drafts exactly the chosen accounts; PW 3 destinations |
| Review: writer, reasoning, cost limit, budget | builder step 4 | `route`, `reasoning`, `maxCostUsdMicro`; weekly ceiling = limit × runs a week | builder | server | paid writer with $0 limit: reminder (every run would be held) | pg cost cap held before provider I/O (existing) |
| Activate / Pause / Resume / Cancel | `AutomationCard` | `raffi_recurrence_activate` / `pause` / `resume` / `cancel` (owner, `confirmed`) | server | server | Cancel asks for confirmation; activation refused without facts or live accounts | py; PW all four |
| Run history, Review latest drafts | `AutomationCard` | reads `occurrences` (state, reason, skipped accounts, conversation) | server | server | held/missed/cancelled runs explained in words | pg; PW run → drafts for both accounts |
| Home panel | `RaffiPlanner` (Home) | reads automations; suggestions unchanged; a campaign suggestion opens `/app/automations?campaign=<id>` | snapshot | — | — | PW Home panel lists the automation and links back |
