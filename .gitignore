.DS_Store
.env
.env.*
!.env.example
.vercel/
__pycache__/
*.py[cod]
.pytest_cache/
studio/web/node_modules/
studio/web/dist-alpha/
.vercel
.env*

# --- added 2026-09-16: local secrets, venvs, build output, broker session dirs ---
.*-broker-*/
broker.key
studio-install-manifest.json
.venv/
.phase3-build-venv/
.phase3-qualification/
.upgrade-backup-*/
node_modules/
studio/web/dist/
studio/broker/node_modules/
*.command

# --- web (Next.js consumer app)
web/node_modules/
web/.next/
!web/.env.example

# --- desktop build output (packaged Electron app, exceeds GitHub file size limits)
desktop/artifacts/

# .env.example is documentation, not a secret (the earlier .env* line re-ignored it)
!.env.example
/.token-pilot/
/.claude/settings.local.json
/.agents/
/.codex/
/AGENTS.md
/CLAUDE.md
/web/AGENTS.md
/web/CLAUDE.md

# scripts/consumer_ready_secrets.py is the offline secret scanner the release gates run (CI): it is source, not a
# credential helper, and must stay tracked.

# Local release-gate evidence (CI uploads it as an artifact)
docs/consumer-ready/evidence/
