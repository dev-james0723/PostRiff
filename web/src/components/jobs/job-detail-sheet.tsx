'use client';

/** The Queue and its draft details use the same responsive receipt and job controls. */
export { JobSheet as JobDetailSheet } from '@/features/queue/job-sheet';
export type { JobSheetProps as JobDetailSheetProps } from '@/features/queue/job-sheet';
